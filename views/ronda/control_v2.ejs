<%- include('../partials/head') %>
    <style>
        /* Custom soft colors for dark mode sticky columns */
        .dark .bg-gray-soft { background-color: rgb(75 75 75); }
        .dark .bg-gray-medium { background-color: rgb(60 60 60); }
        
        .dark .bg-green-soft { background-color: rgb(67 94 77); }
        .dark .bg-blue-soft { background-color: rgb(67 84 94); }
        .dark .bg-red-soft { background-color: rgb(94 67 67); }
        .dark .bg-orange-soft { background-color: rgb(94 80 67); }
        
        .dark .bg-green-medium { background-color: rgb(45 75 55); }
        .dark .bg-blue-medium { background-color: rgb(45 65 75); }
        .dark .bg-red-medium { background-color: rgb(75 45 45); }
        .dark .bg-orange-medium { background-color: rgb(75 60 45); }
        
        /* Dropdown Contrast Fix for Dark Mode */
        .dark select option {
            background-color: #1f2937;
            color: #f3f4f6;
        }
    </style>
    <%- include('../partials/navbar', { useWideContainer: true, user: user }) %>

        <div class="mx-auto py-6 px-2 sm:px-4 lg:px-6">
            <div class="py-6">
                <div class="flex flex-col md:flex-row justify-between items-center mb-6 gap-4">
                    <div class="flex items-center space-x-4">
                        <a href="/ronda" class="text-gray-600 dark:text-gray-400 hover:text-gray-200 transition-colors">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                    d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                            </svg>
                        </a>
                        <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-100 flex items-center gap-3">
                            <span class="text-4xl">📋</span>
                            Control Absensi Ronda
                        </h1>
                    </div>
<script src="https://cdn.jsdelivr.net/npm/qrcode@1.5.1/build/qrcode.min.js"></script>

                    <div class="flex flex-wrap justify-end gap-3">
                        <!-- Export Button Wrapper -->
                        <div class="glass-card p-3 flex items-center">
                            <a href="/ronda/export-control?year=<%= year %>"
                                class="flex items-center gap-2 px-3 py-2 text-sm font-bold text-green-600 dark:text-green-400 bg-green-50 dark:bg-green-900/20 rounded-lg hover:bg-green-100 dark:hover:bg-green-900/30 transition-colors">
                                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path>
                                </svg>
                                Export Excel
                            </a>
                        </div>

                        <div class="glass-card p-4 flex items-center justify-between">
                            <% let prevY=parseInt(year) - 1;
                                let nextY=parseInt(year) + 1; %>
    
                                <a href="/ronda/control?year=<%= prevY %>"
                                    class="p-2 bg-gray-100 dark:bg-white/10 hover:bg-gray-200 dark:hover:bg-white/20 rounded-lg text-gray-600 dark:text-gray-300 transition-colors">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M15 19l-7-7 7-7"></path>
                                    </svg>
                                </a>
    
                                <h2 class="text-xl font-bold text-gray-900 dark:text-gray-100">
                                    Tahun <%= year %>
                                </h2>
                                
                                <a href="/ronda/control?year=<%= nextY %>"
                                    class="p-2 bg-gray-100 dark:bg-white/10 hover:bg-gray-200 dark:hover:bg-white/20 rounded-lg text-gray-600 dark:text-gray-300 transition-colors">
                                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                            d="M9 5l7 7-7 7"></path>
                                    </svg>
                                </a>
                        </div>
                    </div>
                </div>

                <div class="glass-card p-0 overflow-hidden mx-2 sm:mx-4 lg:mx-6">
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-white/10">
                            <thead class="bg-gray-100 dark:bg-white/5 backdrop-blur-xl">
                                <tr>
                                    <th scope="col" onclick="sortTable(0, 'string')"
                                        class="px-2 py-2 sm:px-6 sm:py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider sticky left-0 bg-gray-100 bg-gray-soft z-20 border-r border-gray-300 dark:border-gray-700 cursor-pointer group hover:bg-gray-200 dark:hover:bg-gray-700 transition-colors">
                                        <div class="flex items-center justify-between">
                                            Warga
                                            <span class="sort-icon ml-1 text-gray-900 dark:text-white">↑</span>
                                        </div>
                                    </th>
                                    <% 
                                    // Determine active Saturday (current week's Saturday)
                                    const now = moment();
                                    const currentDay = now.day();
                                    const currentHour = now.hour();
                                    let activeSaturday = null;
                                    
                                    // If it's Sunday before 6 AM, the active Saturday is yesterday
                                    if (currentDay === 0 && currentHour < 6) {
                                        activeSaturday = now.clone().subtract(1, 'days').format('YYYY-MM-DD');
                                    } else {
                                        // Find the Saturday of the current week
                                        const startOfWeek = now.clone().startOf('week');
                                        activeSaturday = startOfWeek.clone().add(6, 'days').format('YYYY-MM-DD');
                                    }
                                    
                                    %>
                                    <th scope="col" onclick="sortTable(1, 'number')"
                                        class="table-cell sm:hidden px-1 py-3 text-center text-[10px] font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-green-100 bg-green-soft w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Total</span>
                                            <span>Denda</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(2, 'number')"
                                        class="table-cell sm:hidden px-1 py-3 text-center text-[10px] font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-blue-100 bg-blue-soft w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Sudah</span>
                                            <span>Bayar</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(3, 'number')"
                                        class="table-cell sm:hidden px-1 py-3 text-center text-[10px] font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-red-100 bg-red-soft w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Ter-</span>
                                            <span>hutang</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(4, 'number')"
                                        class="table-cell sm:hidden px-1 py-3 text-center text-[10px] font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-orange-100 bg-orange-soft w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Tidak</span>
                                            <span>Hadir</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <% 
                                    saturdays.forEach(d=> { 
                                        const isActive = d === activeSaturday;
                                    %>
                                        <th scope="col"
                                        class="px-2 py-2 sm:px-6 sm:py-3 text-center text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider min-w-[120px] <%= isActive ? 'bg-indigo-50 dark:bg-indigo-900/40 border-b-2 border-indigo-500' : '' %>">
                                        <div class="mb-1">Tgl <%= moment(d).format('DD') %></div>
                                        <div class="text-[10px] text-gray-500 dark:text-gray-400">
                                            <%= moment(d).format('MMM') %>
                                        </div>
                                            <% if (isActive) { %>
                                                <div class="text-[10px] font-bold text-indigo-600 dark:text-indigo-300 mt-0.5">● Upcoming</div>
                                            <% } %>
                                        </th>
                                        <% }) %>
                                        
                                    <!-- Desktop Sticky Columns -->
                                    <th scope="col" onclick="sortTable(-4, 'number')"
                                        class="hidden sm:table-cell px-1 py-3 text-center text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-green-100 bg-green-soft border-l-2 border-green-400 sm:sticky sm:right-[270px] z-10 w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Total</span>
                                            <span>Denda</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(-3, 'number')"
                                        class="hidden sm:table-cell px-1 py-3 text-center text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-blue-100 bg-blue-soft sm:sticky sm:right-[180px] z-10 w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Sudah</span>
                                            <span>Bayar</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(-2, 'number')"
                                        class="hidden sm:table-cell px-1 py-3 text-center text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-red-100 bg-red-soft sm:sticky sm:right-[90px] z-10 w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Ter-</span>
                                            <span>hutang</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                    <th scope="col" onclick="sortTable(-1, 'number')"
                                        class="hidden sm:table-cell px-1 py-3 text-center text-xs font-medium text-gray-700 dark:text-gray-300 uppercase tracking-wider bg-orange-100 bg-orange-soft sm:sticky sm:right-0 z-10 w-[90px] min-w-[90px] max-w-[90px] cursor-pointer group hover:brightness-95 transition-all">
                                        <div class="flex flex-col items-center justify-center leading-tight">
                                            <span>Tidak</span>
                                            <span>Hadir</span>
                                            <span class="sort-icon text-gray-400/70 group-hover:text-gray-600 mt-0.5">⇵</span>
                                        </div>
                                    </th>
                                </tr>
                            </thead>
                            <tbody class="bg-gray-100 dark:bg-gray-900 divide-y divide-gray-200 dark:divide-gray-700">
                                <% Object.keys(matrix).forEach(wId=> {
                                    const row = matrix[wId];
                                    %>
                                    <tr class="hover:bg-gray-200 dark:hover:bg-gray-800 transition-colors">
                                        <td data-tim="<%= row.info.tim_ronda %>"
                                            class="px-2 py-2 sm:px-6 sm:py-4 whitespace-nowrap sticky left-0 bg-gray-100 bg-gray-soft z-20 border-r border-gray-300 dark:border-gray-700">
                                            <div class="flex items-center">
                                                <div>
                                                    <div class="text-xs sm:text-sm font-medium text-gray-900 dark:text-gray-100">
                                                        <%= row.info.nama %>
                                                    </div>
                                                    <div class="text-[10px] sm:text-xs text-gray-600 dark:text-gray-400">
                                                        Blok <%= row.info.blok %>-<%= row.info.nomor_rumah %>
                                                                <span
                                                                    class="ml-1 inline-flex items-center px-2 py-0.5 rounded text-[10px] font-medium bg-indigo-500/20 text-indigo-300 border border-indigo-400/30">
                                                                    Tim <%= row.info.tim_ronda || '-' %>
                                                                </span>
                                                    </div>
                                                </div>
                                            </div>
                                        </td>
                                        <% 
                                            // Calculate totals for this warga
                                            let totalDenda = 0;
                                            let sudahBayar = 0;
                                            let terhutang = 0;
                                            let tidakHadir = 0;
                                            
                                            saturdays.forEach(d => {
                                                const cell = row.dates[d];
                                                if (cell.status === 'alpa') {
                                                    tidakHadir++;
                                                }
                                                if (cell.denda > 0) {
                                                    totalDenda += cell.denda;
                                                    if (cell.status_bayar === 'paid') {
                                                        sudahBayar += cell.denda;
                                                    } else {
                                                        terhutang += cell.denda;
                                                    }
                                                }
                                            });
                                        %>
                                        
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-green-50 bg-green-soft w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-gray-900 dark:text-gray-100 text-[10px] sm:text-xs">
                                                <%= totalDenda > 0 ? 'Rp ' + totalDenda.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-blue-50 bg-blue-soft w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= sudahBayar > 0 ? 'text-blue-600 dark:text-blue-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= sudahBayar > 0 ? 'Rp ' + sudahBayar.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-red-50 bg-red-soft w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= terhutang > 0 ? 'text-red-600 dark:text-red-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= terhutang > 0 ? 'Rp ' + terhutang.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-orange-50 bg-orange-soft w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= tidakHadir > 0 ? 'text-orange-600 dark:text-orange-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= tidakHadir > 0 ? tidakHadir + 'x' : '-' %>
                                            </span>
                                        </td>

                                        <% 
                                            saturdays.forEach((d, satIndex)=> {
                                            const cell = row.dates[d];
                                            const isActiveColumn = d === activeSaturday;
                                            
                                            let tdClasses = "px-1 py-2 sm:px-6 sm:py-4 whitespace-nowrap text-center text-sm relative ";
                                            
                                            // Active column highlighting
                                            if (isActiveColumn) {
                                                tdClasses += "bg-indigo-50 dark:bg-indigo-900/20 border-x-2 border-indigo-400 ";
                                            }
                                        %>
                                                    <td class="<%= tdClasses %>">
                                                        <div
                                                            class="relative z-10 inline-flex flex-col items-center justify-center w-full h-full">
                                                            <% if (cell.status) { %>
    <span 
        data-schedule-id="<%= cell.id %>"
        data-status="<%= cell.status %>"
        data-nama="<%= row.info.nama %>"
        data-date="<%= moment(d).format('DD MMM YYYY') %>"
        data-keterangan="<%= cell.keterangan || '' %>"
        onclick="openStatusModal(this.dataset.scheduleId, this.dataset.status, this.dataset.nama, this.dataset.date, this.dataset.keterangan)"
        class="cursor-pointer hover:ring-2 hover:ring-indigo-400 hover:scale-105 transition-all px-2 py-1 rounded text-[10px] sm:text-xs font-bold uppercase mb-1 <%= cell.status === 'hadir' ? 'bg-green-100 text-green-800' : (cell.status === 'izin' ? 'bg-yellow-100 text-yellow-800' : (cell.status === 'alpa' ? 'bg-red-500 text-white' : (cell.status === 'reschedule' ? 'bg-blue-100 text-blue-800' : 'bg-gray-100 text-gray-800'))) %>"
        title="Klik untuk ubah status">
        <%= cell.status %>
    </span>
<% } else if (cell.id) { %>
     <span 
        data-schedule-id="<%= cell.id %>"
        data-status="scheduled"
        data-nama="<%= row.info.nama %>"
        data-date="<%= moment(d).format('DD MMM YYYY') %>"
        data-keterangan=""
        onclick="openStatusModal(this.dataset.scheduleId, this.dataset.status, this.dataset.nama, this.dataset.date, this.dataset.keterangan)"
        class="cursor-pointer hover:bg-gray-200 transition-colors px-2 py-1 rounded text-xs font-bold text-gray-400 uppercase mb-1">
        -
    </span>
<% } %>
<% if (cell.denda > 0) { %>
    <% if (cell.status_bayar === 'paid') { %>
        <span class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-bold bg-green-100 text-green-700 border border-green-200 mt-1 cursor-default">
            ✓ Lunas
        </span>
    <% } else { %>
        <span onclick="event.stopPropagation(); openQrisPayModal('<%= cell.id %>', '<%= cell.denda %>', '<%= row.info.nama %>')" 
              class="inline-flex items-center px-1.5 py-0.5 rounded text-[10px] font-bold bg-red-100 text-red-700 border border-red-200 mt-1 cursor-pointer hover:bg-red-200 hover:scale-105 transition-all shadow-sm">
            💸 Bayar
        </span>
    <% } %>
<% } %>
                                                        </div>
                                                    </td>
                                                    <% }) %>
                                                    
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-green-50 bg-green-soft border-l-2 border-green-400 sm:sticky sm:right-[270px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-gray-900 dark:text-gray-100 text-[10px] sm:text-xs">
                                                <%= totalDenda > 0 ? 'Rp ' + totalDenda.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-blue-50 bg-blue-soft sm:sticky sm:right-[180px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= sudahBayar > 0 ? 'text-blue-600 dark:text-blue-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= sudahBayar > 0 ? 'Rp ' + sudahBayar.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-red-50 bg-red-soft sm:sticky sm:right-[90px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= terhutang > 0 ? 'text-red-600 dark:text-red-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= terhutang > 0 ? 'Rp ' + terhutang.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-orange-50 bg-orange-soft sm:sticky sm:right-0 z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold <%= tidakHadir > 0 ? 'text-orange-600 dark:text-orange-400' : 'text-gray-400' %> text-[10px] sm:text-xs">
                                                <%= tidakHadir > 0 ? tidakHadir + 'x' : '-' %>
                                            </span>
                                        </td>                 
                                    </tr>
                                    <% }) %>
                                    
                                    <!-- Grand Total Row -->
                                    <tr class="bg-gray-200 dark:bg-white/20 font-bold border-t-2 border-gray-400">
                                        <td class="px-2 py-2 sm:px-6 sm:py-4 whitespace-nowrap sticky left-0 bg-gray-200 bg-gray-medium z-20 border-r border-gray-300 dark:border-gray-600">
                                            <div class="text-[10px] sm:text-sm font-bold text-gray-900 dark:text-gray-100 uppercase">
                                                GRAND TOTAL
                                            </div>
                                        </td>
                                        <% 
                                            // Calculate grand totals
                                            let grandTotalDenda = 0;
                                            let grandSudahBayar = 0;
                                            let grandTerhutang = 0;
                                            let grandTidakHadir = 0;
                                            
                                            Object.keys(matrix).forEach(wId => {
                                                const row = matrix[wId];
                                                saturdays.forEach(d => {
                                                    const cell = row.dates[d];
                                                    if (cell.status === 'alpa') {
                                                        grandTidakHadir++;
                                                    }
                                                    if (cell.denda > 0) {
                                                        grandTotalDenda += cell.denda;
                                                        if (cell.status_bayar === 'paid') {
                                                            grandSudahBayar += cell.denda;
                                                        } else {
                                                            grandTerhutang += cell.denda;
                                                        }
                                                    }
                                                });
                                            });
                                        %>
                                        
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-green-100 bg-green-medium border-l-2 border-green-400 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-gray-900 dark:text-gray-100">
                                                <%= grandTotalDenda > 0 ? 'Rp ' + grandTotalDenda.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-blue-100 bg-blue-medium w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-blue-600 dark:text-blue-400">
                                                <%= grandSudahBayar > 0 ? 'Rp ' + grandSudahBayar.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-red-100 bg-red-medium w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-red-600 dark:text-red-400">
                                                <%= grandTerhutang > 0 ? 'Rp ' + grandTerhutang.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="table-cell sm:hidden px-1 py-4 whitespace-nowrap text-center text-sm bg-orange-100 bg-orange-medium w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-orange-600 dark:text-orange-400">
                                                <%= grandTidakHadir > 0 ? grandTidakHadir + 'x' : '-' %>
                                            </span>
                                        </td>

                                        <% 
                                            // Empty cells for Saturday columns
                                            saturdays.forEach(d => {
                                        %>
                                            <td class="px-6 py-4"></td>
                                        <% }) %>
                                        
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-green-100 bg-green-medium border-l-2 border-green-400 sm:sticky sm:right-[270px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-gray-900 dark:text-gray-100">
                                                <%= grandTotalDenda > 0 ? 'Rp ' + grandTotalDenda.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-blue-100 bg-blue-medium sm:sticky sm:right-[180px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-blue-600 dark:text-blue-400">
                                                <%= grandSudahBayar > 0 ? 'Rp ' + grandSudahBayar.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-red-100 bg-red-medium sm:sticky sm:right-[90px] z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-red-600 dark:text-red-400">
                                                <%= grandTerhutang > 0 ? 'Rp ' + grandTerhutang.toLocaleString('id-ID') : '-' %>
                                            </span>
                                        </td>
                                        <td class="hidden sm:table-cell px-1 py-4 whitespace-nowrap text-center text-sm bg-orange-100 bg-orange-medium sm:sticky sm:right-0 z-10 w-[90px] min-w-[90px] max-w-[90px]">
                                            <span class="font-bold text-[10px] sm:text-sm text-orange-600 dark:text-orange-400">
                                                <%= grandTidakHadir > 0 ? grandTidakHadir + 'x' : '-' %>
                                            </span>
                                        </td>
                                    </tr>
                            </tbody>
                        </table>
                    </div>

                    <div class="px-6 py-4 bg-gray-50 border-t border-gray-200">
                        <div class="text-xs text-gray-500">
                            <strong>Aturan Denda:</strong> Warga yang tidak hadir jadwal ronda dan tidak melakukan
                            reschedule sampai 4 jadwal ronda berikutnya akan dikenakan sanksi denda.
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Pay Fine Modal -->
        <div id="payFineModal" class="fixed inset-0 bg-black/60 hidden overflow-y-auto h-full w-full z-50 backdrop-blur-sm">
            <div class="relative top-20 mx-auto p-6 glass-card w-full max-w-md shadow-2xl">
                <h3 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-6 flex items-center gap-2">
                    <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    Bayar Denda Ronda
                </h3>
                <form action="/ronda/update-fine-status" method="POST">
                    <input type="hidden" id="fineIds" name="ids">
                    <div class="mb-5">
                        <label class="block text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-2">Nama Warga</label>
                        <div id="fineWargaName" class="text-gray-900 dark:text-gray-100 font-bold bg-gray-100 dark:bg-white/5 p-3 rounded-xl border border-white/10"></div>
                    </div>
                    <div class="mb-5">
                         <label class="block text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-2">Total Tagihan</label>
                        <div class="text-2xl font-bold text-red-500" id="fineAmountDisplay"></div>
                    </div>
                    <div class="mb-6 bg-yellow-50 dark:bg-yellow-900/20 p-4 rounded-xl border border-yellow-200 dark:border-yellow-700/30">
                        <p class="text-sm text-yellow-800 dark:text-yellow-200">
                            <strong>Konfirmasi:</strong> Apakah Anda yakin ingin menandai denda ini sebagai <strong>LUNAS</strong>? Data akan dicatat di Kas.
                        </p>
                    </div>
                    <div class="flex justify-end gap-3">
                        <button type="button" onclick="document.getElementById('payFineModal').classList.add('hidden')"
                            class="px-6 py-2.5 rounded-xl text-sm font-bold text-gray-600 dark:text-gray-400 hover:text-gray-200 transition-colors">
                            Batal
                        </button>
                        <button type="submit"
                            class="bg-green-600 hover:bg-green-700 text-white px-8 py-2.5 rounded-xl font-bold shadow-lg transition-all">
                            Bayar & Lunas
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Status Modal -->
        <div id="statusModal" class="fixed inset-0 bg-black/60 hidden overflow-y-auto h-full w-full z-50 backdrop-blur-sm">
            <div class="relative top-20 mx-auto p-6 glass-card w-full max-w-md shadow-2xl">
                <h3 class="text-lg font-bold text-gray-900 dark:text-gray-100 mb-6 flex items-center gap-2">
                    <svg class="w-5 h-5 text-indigo-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z"></path>
                    </svg>
                    Ubah Status Absensi
                </h3>
                <form action="/ronda/update-status" method="POST">
                    <input type="hidden" id="statusScheduleId" name="id">
                    <input type="hidden" name="redirectUrl" value="/ronda/control?year=<%= year %>">
                    
                    <div class="mb-4">
                        <label class="block text-xs font-bold text-gray-500 dark:text-gray-400 uppercase tracking-wider mb-1">Nama Warga</label>
                        <div id="statusWargaName" class="text-base font-bold text-gray-900 dark:text-gray-100"></div>
                        <div id="statusDateLabel" class="text-xs text-indigo-500 font-medium"></div>
                    </div>

                    <div class="mb-5">
                        <label class="block text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-2">Status Kehadiran</label>
                        <select name="status" id="statusSelect" class="glass-input block w-full rounded-xl p-3 outline-none cursor-pointer text-gray-900 dark:text-gray-100 bg-white dark:bg-gray-800 focus:ring-2 focus:ring-indigo-500 border border-gray-200 dark:border-gray-700 dark:[color-scheme:dark]">
                            <option value="scheduled">Scheduled (Reset)</option>
                            <option value="hadir">Hadir</option>
                            <option value="alpa">Alpa (Denda)</option>
                            <option value="reschedule">Reschedule</option>
                        </select>
                    </div>
                    
                    <div class="mb-6">
                        <label class="block text-xs font-bold text-gray-600 dark:text-gray-400 uppercase tracking-wider mb-2">Keterangan</label>
                        <textarea name="keterangan" id="statusKeterangan" rows="3" placeholder="Tambahkan catatan jika diperlukan..." class="glass-input block w-full rounded-xl p-3 outline-none resize-none text-gray-900 dark:text-gray-100 focus:ring-2 focus:ring-indigo-500"></textarea>
                    </div>

                    <div class="flex justify-end gap-3 pt-4 border-t border-gray-100 dark:border-white/10">
                        <button type="button" onclick="document.getElementById('statusModal').classList.add('hidden')" class="px-5 py-2.5 rounded-xl text-sm font-bold text-gray-600 dark:text-gray-400 hover:text-gray-800 hover:bg-gray-100 dark:hover:bg-white/10 transition-colors">Batal</button>
                        <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-6 py-2.5 rounded-xl font-bold shadow-lg transition-all transform hover:scale-[1.02]">Simpan Perubahan</button>
                    </div>
                </form>
            </div>
        </div>

        <script>
            let sortDirection = 1; // 1 asc, -1 desc
            let lastColIndex = -1; // Reset to -1 to ensure first click or auto-sort triggers ASC

            document.addEventListener('DOMContentLoaded', () => {
                // Force sort by Warga (Tim Ronda) ASC on load
                sortTable(0, 'string');
            });

            function sortTable(colIndexRelative, type) {
                const table = document.querySelector('table');
                const tbody = table.querySelector('tbody');
                // All rows except the last one (Grand Total)
                const rows = Array.from(tbody.querySelectorAll('tr')).slice(0, -1);
                // The Grand Total row (to insert before)
                const grandTotalRow = tbody.querySelector('tr:last-child');
                const headerRow = table.querySelector('thead tr');
                
                // Calculate absolute column index
                // For negative index, it counts from the end
                let colIndex = colIndexRelative;
                if (colIndexRelative < 0) {
                    // Total columns in the header row
                    const totalCols = headerRow.children.length;
                    colIndex = totalCols + colIndexRelative;
                }

                // Toggle logic
                if (lastColIndex === colIndex) {
                    sortDirection *= -1;
                } else {
                    sortDirection = 1;
                    lastColIndex = colIndex;
                }

                // Reset all icons
                document.querySelectorAll('.sort-icon').forEach(icon => {
                    icon.textContent = '⇵';
                    icon.classList.remove('text-gray-900', 'dark:text-white', 'text-gray-400');
                    icon.classList.add('text-gray-400');
                });
                
                // Update active icon
                const targetHeader = headerRow.children[colIndex];
                if(targetHeader) {
                    const icon = targetHeader.querySelector('.sort-icon');
                    if(icon) {
                        icon.textContent = sortDirection === 1 ? '↑' : '↓';
                        icon.classList.remove('text-gray-400');
                        icon.classList.add('text-gray-900', 'dark:text-white');
                    }
                }

                // Sort rows
                rows.sort((a, b) => {
                    const cellA = a.children[colIndex];
                    const cellB = b.children[colIndex];
                    
                    if (!cellA || !cellB) return 0;

                    const valA = getCellValue(cellA, type);
                    const valB = getCellValue(cellB, type);
                    
                    if (valA < valB) return -1 * sortDirection;
                    if (valA > valB) return 1 * sortDirection;
                    return 0;
                });

                // Re-append sorted rows
                rows.forEach(row => tbody.insertBefore(row, grandTotalRow));
            }

            function getCellValue(cell, type) {
                // Priority: Custom sort attribute for Tim Ronda (Warga column)
                if (cell.hasAttribute('data-tim')) {
                    const val = cell.getAttribute('data-tim');
                    // Put empty values at the end when ascending ('z')
                    return val ? val.toLowerCase() : 'z';
                }

                let text = cell.innerText.trim(); 
                
                if (type === 'number') {
                    // Remove 'Rp', '.', ' ', 'x', '-'
                    const cleanText = text.replace(/[^0-9-]/g, ''); 
                    if (!cleanText || cleanText === '-') return 0;
                    return parseInt(cleanText, 10);
                }
                
                // Fallback for other string columns
                return text.toLowerCase();
            }

            function openStatusModal(id, currentStatus, name, date, keterangan) {
                document.getElementById('statusScheduleId').value = id;
                document.getElementById('statusWargaName').textContent = name;
                document.getElementById('statusDateLabel').textContent = date;
                document.getElementById('statusSelect').value = currentStatus && currentStatus !== '' ? currentStatus : 'scheduled';
                document.getElementById('statusKeterangan').value = keterangan || '';
                document.getElementById('statusModal').classList.remove('hidden');
            }

            function openPayFineModal(ids, amount, name) {
                document.getElementById('fineIds').value = ids;
                document.getElementById('fineAmountDisplay').textContent = 'Rp ' + parseInt(amount).toLocaleString('id-ID');
                document.getElementById('fineWargaName').textContent = name;
                document.getElementById('payFineModal').classList.remove('hidden');
            }
            
            function openQrisPayModal(id, denda, name) {
                document.getElementById('qrisFineId').value = id;
                document.getElementById('qrisAmountDisplay').innerText = 'Rp ' + parseInt(denda).toLocaleString('id-ID');
                document.getElementById('qrisModal').classList.remove('hidden');

                // Generate QRIS
                const qrisData = "<%= typeof staticQris !== 'undefined' ? staticQris : '' %>" + id;
                const canvas = document.getElementById('qrisCanvas');
                
                // Clear previous
                const context = canvas.getContext('2d');
                context.clearRect(0, 0, canvas.width, canvas.height);

                QRCode.toCanvas(canvas, qrisData, { width: 200, margin: 2 }, function (error) {
                    if (error) console.error(error);
                });
            }
        </script>

        <!-- QRIS Pay Modal -->
        <div id="qrisModal" class="fixed inset-0 z-[10000] hidden overflow-y-auto" aria-labelledby="modal-title" role="dialog" aria-modal="true">
            <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
                <div class="fixed inset-0 bg-gray-900 bg-opacity-75 transition-opacity" aria-hidden="true" onclick="document.getElementById('qrisModal').classList.add('hidden')"></div>
                <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
                <div class="inline-block align-bottom bg-white dark:bg-gray-800 rounded-lg text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full border border-gray-200 dark:border-gray-700">
                    <div class="px-4 pt-5 pb-4 sm:p-6 sm:pb-4">
                        <h3 class="text-lg leading-6 font-medium text-gray-900 dark:text-gray-100" id="modal-title">Bayar Denda Ronda</h3>
                        <div class="mt-4 text-center">
                            <p class="text-sm text-gray-600 dark:text-gray-400 mb-4">Scan QRIS di bawah ini untuk membayar denda sebesar <span class="font-bold text-red-500" id="qrisAmountDisplay"></span></p>
                            <div class="flex justify-center bg-white p-4 rounded-xl mx-auto w-fit border border-gray-200">
                                <canvas id="qrisCanvas"></canvas>
                            </div>
                        </div>
                        <form action="/ronda/pay-fine-upload" method="POST" enctype="multipart/form-data" class="mt-4">
                            <input type="hidden" name="id" id="qrisFineId">
                            <input type="hidden" name="redirectUrl" value="/ronda/control?year=<%= year %>">
                            <div class="mb-4">
                                <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Upload Bukti Transfer</label>
                                <input type="file" name="foto_bukti" accept="image/*" required class="w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-indigo-50 file:text-indigo-700 hover:file:bg-indigo-100">
                            </div>
                            <div class="mt-5 sm:mt-6 sm:grid sm:grid-cols-2 sm:gap-3 sm:grid-flow-row-dense">
                                <button type="submit" class="w-full inline-flex justify-center rounded-md border border-transparent shadow-sm px-4 py-2 bg-indigo-600 text-base font-medium text-white hover:bg-indigo-700 focus:outline-none sm:col-start-2 sm:text-sm">
                                    Kirim Bukti
                                </button>
                                <button type="button" onclick="document.getElementById('qrisModal').classList.add('hidden')" class="mt-3 w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none sm:mt-0 sm:col-start-1 sm:text-sm">
                                    Batal
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>

        <%- include('../partials/footer') %>